function formatDuration(seconds){seconds=Math.max(0,Math.floor(seconds));const days=Math.floor(seconds/86400);seconds%=86400;const hours=Math.floor(seconds/3600);seconds%=3600;const minutes=Math.floor(seconds/60);seconds%=60;const parts=[];if(days)parts.push(`${days}d`);if(hours||days)parts.push(`${hours}h`);if(minutes||hours||days)parts.push(`${minutes}m`);parts.push(`${seconds}s`);return parts.join(" ");}
function startOfDay(timestamp=Date.now()){const d=new Date(timestamp);d.setHours(0,0,0,0);return d.getTime();}
function startOfWeek(timestamp=Date.now()){const d=new Date(timestamp);d.setHours(0,0,0,0);const day=d.getDay();const difference=day===0?6:day-1;d.setDate(d.getDate()-difference);return d.getTime();}
function startOfMonth(timestamp=Date.now()){const d=new Date(timestamp);d.setHours(0,0,0,0);d.setDate(1);return d.getTime();}
function rangeSeconds(memberData,rangeStart,now=Date.now()){let total=0;for(const session of memberData.sessions||[]){const start=Math.max(session.start,rangeStart),end=Math.min(session.end??now,now);if(end>start)total+=(end-start)/1000;}if(memberData.activeSince){const start=Math.max(memberData.activeSince,rangeStart);if(now>start)total+=(now-start)/1000;}return total;}
module.exports={formatDuration,startOfDay,startOfWeek,startOfMonth,rangeSeconds};
